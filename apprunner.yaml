version: 1.0
runtime: go1

build:
  commands:
    build:
      - go build -o main .

run:
  command: ./main
  network:
    port: 8080
  env:
    - name: ENV
      value: "production"
    - name: PORT
      value: "8080"

